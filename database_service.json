{
  "serviceName": "food-database-service",
  "taskDefinition": "food-database",
  "cluster": "your-ecs-cluster-name",
  "launchType": "FARGATE",
  "desiredCount": 1,
  "networkConfiguration": {
    "awsvpcConfiguration": {
      "securityGroups": []  // No need to specify security groups for default VPC
    }
  }
}
